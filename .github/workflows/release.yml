name: Release
on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  github-action:
    uses: cloudposse/.github/.github/workflows/shared-release-branches.yml@fcbe2ddadb80fa7f643d2a5cb703c31d3e5bbf05 # main
    secrets: inherit
